

//Trigger function which triggers after user record got inserted into users table .
// what it does is it inserts user_id and current_timestamps into logs table

// BEGIN
// INSERT INTO logs (user_id,user_created)
// VALUES(new.ID,current_timestamp);
// RETURN NEW;
// END;


